Each folder contains the implementation of each algorithm. The number at the end describes how many encryptions are performed in parallel. ARM implementations have four encryptions in parallel, the Skinny implementation runs with default settings for Skinny.

Thesis.pdf is my master's thesis